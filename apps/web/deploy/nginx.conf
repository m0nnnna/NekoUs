# Serves the built SPA, falling back to index.html for client-side routing (react-router-dom).
server {
  listen 80;
  listen [::]:80;

  root /usr/share/nginx/html;

  location / {
    try_files $uri $uri/ /index.html;
  }
}
