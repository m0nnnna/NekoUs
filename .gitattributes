# Shell scripts run on Linux (the server, and inside the web image via nginx's entrypoint), where a
# CRLF line ending breaks them. Keep them LF in every checkout, whatever core.autocrlf says.
*.sh text eol=lf
